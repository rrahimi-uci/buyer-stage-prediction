"""tabular-automl-template — a domain-agnostic tabular AutoML batch-scoring pipeline.

Framework code in this package contains ZERO domain knowledge. All domain specifics
(target, labels, windows, feature spec, label join) live under ``examples/<domain>/``.
"""

__version__ = "0.1.0"
